# ==== Django ====
# Genera tu clave con:
# python3 -c "from django.core.management.utils import get_random_secret_key as g; print(g())"
SECRET_KEY="pon_aqui_tu_secret_key"

# 1 = Debug activado (desarrollo), 0 = Debug desactivado (producción)
DJANGO_DEBUG=1

# Coma-separado. Ejemplo: "127.0.0.1,localhost,mi-dominio.com"
ALLOWED_HOSTS=127.0.0.1,localhost

# ==== OpenAI ====
# Clave de API de OpenAI
OPENAI_API_KEY="sk-xxxx"

# Modelos por defecto (puedes cambiarlos si lo deseas)
OPENAI_TEXT_MODEL="gpt-4o-mini"
OPENAI_EMB_MODEL="text-embedding-3-small"

# Si tu organización NO tiene acceso a imágenes, deja esto vacío o comenta la línea
# OPENAI_IMAGE_MODEL="gpt-image-1"
#OPENAI_IMAGE_MODEL=""
